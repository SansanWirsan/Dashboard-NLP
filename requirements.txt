streamlit>=1.32
pandas>=2.0
numpy>=1.24
scikit-learn>=1.3
nltk>=3.8
plotly>=5.18
matplotlib>=3.7
seaborn>=0.13

# Balancing
imbalanced-learn>=0.12
deep-translator>=1.11        # back translation (butuh internet)

# Word embeddings (non-contextual)
gensim>=4.3                  # Word2Vec, FastText, GloVe (via gensim.downloader)

# Contextual embeddings (BERT / RoBERTa / DeBERTa via sentence-transformers)
sentence-transformers>=2.7
torch>=2.2
